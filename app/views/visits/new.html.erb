<%= simple_form_for [@dog, @visit] do |f| %>
  <%= f.date_field :date, as: :date, min: Date.today %>
  <%= f.button :submit %>
<% end %>
<%= link_to "Back to #{@dog.name}", @dog %>
<%= link_to "Back to #{@dog.name} visits", dog_visits_path(@dog) %>
